{
  "name": "dlx-sv-root",
  "private": true,
  "scripts": {
    "dev": "bun run --cwd app dev",
    "build": "bun run --cwd app build",
    "build:web": "bun run --cwd app build:web",
    "build:server": "bun run --cwd app build:server",
    "check": "bun run --cwd app check",
    "test:e2e": "bun run --cwd app test:e2e",
    "render:py-mp4": "bun run scripts/render-py-mp4.ts",
    "check:parity:dlxn": "bun run --cwd app check:parity:dlxn"
  }
}
